export function ProviderRegistrationLoadingState({ text }: { text: string }) {
  return (
    <div className="grid min-h-[420px] place-items-center text-sm font-semibold text-muted">
      {text}
    </div>
  );
}
