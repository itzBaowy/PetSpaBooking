export type ProviderDisputeStatus = "open" | "awaiting_provider" | "awaiting_customer" | "under_review" | "resolved_customer" | "resolved_provider" | "closed";
export interface ProviderDisputeEvidenceMock { id: string; filename: string; type: "image" | "document"; uploadedBy: "customer" | "provider"; uploadedAt: string; url?: string; isLocal?: boolean; }
export interface ProviderDisputeTimelineMock { id: string; title: string; description: string; actor: string; occurredAt: string; }
export interface ProviderDisputeMock { id: string; code: string; booking: { id: string; code: string; service: string; appointmentAt: string; totalAmount: number }; customer: { name: string; phone: string; email: string }; reason: string; disputedAmount: number; amountOnHold: number; createdAt: string; responseDeadline: string; status: ProviderDisputeStatus; customerClaim: string; customerEvidence: ProviderDisputeEvidenceMock[]; providerResponse?: string; providerNote?: string; providerEvidence: ProviderDisputeEvidenceMock[]; timeline: ProviderDisputeTimelineMock[]; adminDecision?: { outcome: string; summary: string; decidedAt: string }; }
